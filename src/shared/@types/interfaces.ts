interface FilterItem {
  id: number;
  title: string;
  selection: boolean;
}

interface TourItem {
  id: number;
  title: string;
  selection: boolean;
  location: string;
  rating: number;
  image: any;
}
